import React from 'react';


const Cart = (props) => {

    const { image , title , price , quantity } = props.itemData
 
    return (
        <div>
        
        </div>
    );
};

export default Cart;